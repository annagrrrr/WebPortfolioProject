<section class="default-section">
  <div class="dashboard-projects-wrapper">
    <h2 class="dashboard-header">Projects</h2>
    <ul class="dashboard-projects">
      <% data.forEach(project => { %>
        <li data-id="<%= project._id %>">
          <div class="dashboard-project">
            <div class="project-view">
              <a href="/project/<%= project._id %>">
                <h3 class="project-name"><%= project.name %></h3>
              </a>
              <p class="project-description"><%= project.description %></p>
              <p class="project-additionalinfo"><%= project.additionalinfo %></p>
              <img src="<%= project.cover %>" alt="Cover">
              <div class="dashboard-project-controls">
                <button class="edit-btn dashboard-btn btn btn-border">Edit</button>
                <button class="dashboard-btn btn delete-btn btn-border" data-id="<%= project._id %>">Delete</button>
              </div>
              <div class="old-project-data hidden">
                <p class="old-name old-field"></p>
                <p class="old-description old-field"></p>
                <p class="old-additionalinfo old-field"></p>
                <img class="old-cover old-field" src="">
              </div>
            </div>

            <form class="project-edit-form hidden">
              <input type="text" name="name" value="<%= project.name %>">
              <textarea name="description"><%= project.description %></textarea>
              <textarea name="additionalinfo"><%= project.additionalinfo %></textarea>
              <input type="text" name="cover" value="<%= project.cover %>">
              <div class="form-buttons">
                <button type="button" class="save-btn dashboard-btn btn btn-border">Save</button>
                <button type="button" class="cancel-btn dashboard-btn btn btn-border">Cancel</button>
              </div>
            </form>
          </div>
        </li>
      <% }) %>
    </ul>
  </div>

  <h2 class="add-project-header">Add project</h2>
  <form action="/add-project" method="POST" class="add-project">
    <div class="add-project-input-box">
      <input type="text" placeholder="Name" name="name">
    </div>
    <div class="add-project-input-box">
      <textarea cols="50" rows="10" placeholder="Description" name="description"></textarea>
    </div>
    <div class="add-project-input-box">
      <textarea cols="50" rows="5" placeholder="Additional info" name="additionalinfo"></textarea>
    </div>
    <div class="add-project-input-box">
      <input type="text" placeholder="Cover (/img/<your_image>.png)" name="cover">
    </div>
    <input type="submit" value="Add" class="add-project-button btn">
</form>

</section>
